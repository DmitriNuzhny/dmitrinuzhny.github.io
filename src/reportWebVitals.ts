const reportWebVitals = (onPerfEntry?: any) => {
  // Implementation will be added when web-vitals is installed properly
  console.log('Web vitals reporting is disabled');
};

export default reportWebVitals;
